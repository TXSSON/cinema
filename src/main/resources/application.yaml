server:
  port: 8080
  servlet:
    context-path: /api/v1
    encoding:
      charset: UTF-8
      enabled: true
      force: true
spring:
  application:
    name: cinema
  datasource:
    url: jdbc:mysql://localhost:3306/cinema?useSSL=false&serverTimezone=UTC
    username: root
    password: Txshust2004@
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true



